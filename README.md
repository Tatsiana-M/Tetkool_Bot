# Tetkool Bot 🤖

AI-консультант для бизнеса и продажи образовательных курсов в Telegram. Бот помогает пользователям подобрать подходящее обучение, отвечает на вопросы и собирает заявки.

## 🚀 Возможности

*   **Умный диалог**: Ведет пользователя по воронке продаж (Знакомство -> Диагностика -> Подбор решения -> Продажа), используя OpenAI GPT-4o.
*   **База знаний**: Предоставляет актуальную информацию о курсах (стоимость, длительность, даты старта) из `knowledge_base.json`.
*   **Связь с менеджером**: Собирает контакты и имя пользователя для отправки заявки менеджеру на Email.
*   **Обработка возражений**: Грамотно отвечает на сомнения ("Дорого", "Нет времени").
*   **Выбор LLM**: Поддержка OpenAI и OpenRouter.

## 🛠 Технологический стек

*   **Python 3.10+**
*   **Aiogram 3.x** — асинхронный фреймворк для Telegram Bot API.
*   **OpenAI API / OpenRouter** — для генерации ответов.
*   **SMTP** — для отправки Email уведомлений.

## 📂 Структура проекта

```
Tetkool_Bot/
├── main.py                 # Точка входа, запуск бота и логика aiogram
├── system_prompt.txt       # Сценарий и инструкции для LLM
├── knowledge_base.json     # Данные о курсах (JSON)
├── requirements.txt        # Зависимости
├── .env                    # Переменные окружения (токены, пароли)
└── tools/                  # Инструменты для LLM
    ├── get_courses.py      # Поиск курсов в базе знаний
    └── send_email.py       # Отправка Email менеджеру
```

## ⚙️ Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/your-username/tetkool-bot.git
    cd Tetkool_Bot
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python3 -m venv venv
    source venv/bin/activate  # Для Mac/Linux
    # venv\Scripts\activate   # Для Windows
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройте переменные окружения:**
    Создайте файл `.env` на основе `.env.example` и заполните его:
    ```ini
    TELEGRAM_BOT_TOKEN=ваш_токен_бота
    OPENAI_API_KEY=ваш_ключ_openai
    # Или OPENROUTER_API_KEY=ваш_ключ_openrouter
    
    # Настройки почты (Gmail App Password)
    GMAIL_USER=ваш_email@gmail.com
    GMAIL_APP_PASSWORD=пароль_приложения
    MANAGER_EMAIL=куда_отправлять_заявки@gmail.com
    ```

5.  **Запустите бота:**
    ```bash
    python3 main.py
    ```

## 📝 Использование

*   Напишите `/start` боту в Telegram.
*   Отвечайте на вопросы диагностики.
*   Бот подберет курс и предложит решение.
*   Если у вас возникнет сложный вопрос или желание купить, бот запросит имя и контакт, чтобы передать заявку менеджеру.

## 🔧 Дополнительно

*   **Изменение курсов**: Редактируйте файл `knowledge_base.json`.
*   **Изменение сценария**: Редактируйте `system_prompt.txt`.
